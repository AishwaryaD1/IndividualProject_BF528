#!/usr/bin/env python
# concatenate.py

# Iterate overa range of values
for num in range(1,10):
    if num % 2 == 0:
        print(str(num) + ' is even')
    else:
        print(str(num) + ' is odd') 
